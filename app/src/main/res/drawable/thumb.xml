<?xml version="1.0" encoding="utf-8"?><!-- res/drawable/thumb_material.xml -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item>
        <shape android:shape="oval">
            <solid android:color="@color/white" />
            <size
                android:width="12dp"
                android:height="12dp" />  <!-- Smaller thumb size -->
            <padding
                android:bottom="4dp"
                android:left="4dp"
                android:right="4dp"
                android:top="4dp" />  <!-- Padding for the ripple effect -->
        </shape>
    </item>
</ripple>
